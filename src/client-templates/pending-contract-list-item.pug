.row.contract-list-item(data-contract=JSON.stringify(contract))
  .col
    span
      =contract.itemGroup.displayName || contract.itemGroup.name
  .col
    if contract.status == 'DRAFT'
      span Tarkasta Ehdotus
    else if contract.status == 'ON_HOLD'
      span Pakkasmarjan tarkistettavana
    else if contract.status == 'REJECTED'
      span Hylätty
    else
      span Sopimus käsittelyssä
